# Generated by nginxconfig.io
# https://www.serverion.com/nginx-config/#?0.domain=sha.smartpace.co.uk&0.path=%2Fhome%2Fal-shamelah%2Fbackend%2Falshamelah_api&0.document_root=%2F&0.redirect=false&0.https=false&0.force_https=false&0.php=false&0.django&0.proxy&0.proxy_pass=http:%2F%2F127.0.0.1:8000&0.root=false&0.access_log_domain&0.error_log_domain

user www-data;
pid /run/nginx.pid;
worker_processes auto;
worker_rlimit_nofile 65535;

events {
	multi_accept on;
	worker_connections 65535;
}

http {
	charset utf-8;
	sendfile on;
	tcp_nopush on;
	tcp_nodelay on;
	server_tokens off;
	log_not_found off;
	types_hash_max_size 2048;
	client_max_body_size 200M;

	# MIME
	include mime.types;
	default_type application/octet-stream;

	# logging
	access_log /var/log/nginx/access.log;
	error_log /var/log/nginx/error.log warn;

	# load configs
	include /etc/nginx/conf.d/*.conf;
	include /etc/nginx/sites-enabled/*;
}
